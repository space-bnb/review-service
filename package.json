{
  "name": "we-work-reviews",
  "version": "",
  "description": "",
  "author": "",
  "license": "",
  "engines": {
    "node": ">=6.13.0"
  },
  "scripts": {
    "dev": "npm run seed && nodemon server/serverStart.js",
    "setup": "npm install && npm run seed && npm run build",
    "seed": "node server/db/data/seed.js -s",
    "data-generate": "node server/dataGeneration/index.js && node server/dumps/execCat.js",
    "build": "webpack",
    "dev-build": "webpack --watch",
    "test-server": "npm run seed && jest --detectOpenHandles --testEnvironment=node --verbose ./server/__tests__",
    "test-unit": "jest ./client/spec/unit",
    "shell": "node server/dumps/execCat.js"
  },
  "dependencies": {
    "aws-sdk": "^2.839.0",
    "axios": "^0.21.1",
    "cors": "^2.8.5",
    "enzyme": "^3.11.0",
    "express": "^4.17.1",
    "grunt": "^1.3.0",
    "jest": "^26.6.3",
    "mongoose": "^5.11.12",
    "nodemon": "^2.0.7",
    "supertest": "^6.1.3"
  }
}
